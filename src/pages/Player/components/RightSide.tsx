import React from 'react'
import { PropsSideInterface } from '../types'

export const RightSide = (props: PropsSideInterface) => {
  return(
    <div>
      RightSide
    </div>
  )
}

export default RightSide